<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"> <!-- Arrière-plan pour la PopupWindow -->

    <!-- ScrollView pour permettre le défilement du contenu -->
    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- LinearLayout pour contenir les éléments du formulaire -->
        <LinearLayout
            android:id="@+id/linearLayoutForm"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- EditText pour écrire le post -->
            <!-- Marge en bas pour l'espacement -->

            <!-- LinearLayout pour les boutons Annuler et Publier -->
            <EditText
                android:id="@+id/editTextPostContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:hint="what's on your mind?..."
                android:inputType="textMultiLine"
                android:maxLines="1000"
                android:minHeight="100dp"
                android:minLines="10"
                android:scrollbars="vertical" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:gravity="end"
                android:orientation="horizontal">

                <!-- Bouton Annuler -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/buttonCancel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:text="CANCEL"
                    android:textColor="@color/white"
                    app:backgroundTint="@color/darkGray"
                    app:cornerRadius="8dp"
                    app:strokeColor="@color/white"
                    app:strokeWidth="2dp" />

                <!-- Bouton Publier -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/buttonPost"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="POST IT"
                    android:textColor="@color/white"
                    app:backgroundTint="@color/purple"
                    app:cornerRadius="8dp"
                    app:strokeColor="@color/white"
                    app:strokeWidth="2dp" />
            </LinearLayout>
        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>